using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class TitleScreenScript : MonoBehaviour {

/*    [SerializeField]
    Image m_attachedText;

    [SerializeField]
    bool growing;

	// Use this for initialization
	void Start () {
        m_attachedText = gameObject.GetComponent<Image>();
        growing = true;
	}
	
	// Update is called once per frame
	void Update () {
        if (growing == true)
        {
            gameObject.GetComponent<RectTransform>().sizeDelta. = gameObject.GetComponent<RectTransform>().sizeDelta.x + 0.006f;
        }
        else if (growing == false)
        {
            m_attachedText.characterSize = m_attachedText.characterSize - 0.006f;
        }

        if(m_attachedText.characterSize > 1.6f)
        {
            growing = false;
        }
        else if(m_attachedText.characterSize < 0.8f)
        {
            growing = true;
        }
    }*/
}
